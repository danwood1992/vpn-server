services:
  openvpn:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vpn_server
    env_file:
      - .env
    volumes:
      - ./openvpn-data:/etc/openvpn/data
    ports:
      - "1194:1194/udp"
    cap_add:
      - NET_ADMIN
    restart: always
